function setup() {
  createCanvas(400, 400);
  
}

function draw() {
  background(220);
}

//Challenges:

//1. Create a group of ellipse sprites horizontally

//2. Create a group of rectangle sprites vertically

//3. Use indexing to change a property of ONE of your sprites in each group.

//4. Create a non-Grouped sprite.

//5. Have the non-Grouped sprite interact with the Groups you created in some way (think: movement of group and collision)